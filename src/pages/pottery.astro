---
import BaseLayout from "../layouts/BaseLayout.astro";
import { Image } from "astro:assets";
import type { ImageMetadata } from "astro";
import { debugWarn } from '../lib/debug';

// Import all images from the pottery folder
const imageModules = import.meta.glob<{ default: ImageMetadata }>(
  "../images/pottery/*.{jpeg,jpg,png,gif,webp}",
  { eager: true }
);

// Import raw image URLs for lightbox (unprocessed)
const rawImages = import.meta.glob<string>(
  "../images/pottery/*.{jpeg,jpg,png,gif,webp}",
  { eager: true, import: 'default', query: '?url' }
);

// Validate image modules and filter out any failed loads
const validImageModules = Object.entries(imageModules).filter(([path, module]) => {
  if (!module?.default) {
    if (import.meta.env.DEV) {
      console.error(`[Pottery] Failed to load image: ${path}`);
    }
    return false;
  }
  return true;
});

// Convert to array and extract label from filename
// CRITICAL FIX: Sort paths to ensure consistent order
// This guarantees imageModules and rawImages are accessed in the same order
const images = validImageModules
  .sort(([pathA], [pathB]) => pathA.localeCompare(pathB))
  .map(([path, module]) => {
    const filename = path.split("/").pop() || "";
    const label = filename.replace(/\.(jpeg|jpg|png|gif|webp)$/i, "");
    const description = `Handmade ceramic pottery: ${label}`;
    const rawPath = rawImages[path]; // Get unprocessed URL for lightbox

    // Add defensive check to catch mismatches
    if (!rawPath) {
      console.error(`[Pottery] Missing raw image for: ${path}`);
      return null;
    }

    return {
      src: module.default,    // ImageMetadata for optimized thumbnails
      rawSrc: rawPath,        // String URL for lightbox (unprocessed)
      label,                   // Filename without extension - single source of truth
      filename,
      description,             // Alt text for accessibility
    };
  })
  .filter(Boolean); // Filter out any null entries from missing raw images

// Note: Shuffle moved to client-side for static site deployment
// Server-side shuffle only runs once at build time, so all visitors see same order
// Client-side shuffle runs in browser on each page load
---

<BaseLayout
  title="Handmade Ceramic Pottery by John Dorion"
  description="Wheel-thrown ceramic art including bowls, mugs, vases, and functional pottery pieces. Explore my handcrafted ceramics featuring unique glazes and traditional techniques."
  showCCLicense={true}
  disableViewTransitions={true}
>
  <div class="mb-5 flex justify-between items-start">
    <h1 class="text-3xl w-full font-bold">Pottery</h1>
    <button id="sidebar-toggle" class="btn btn-sm btn-ghost hidden lg:flex gap-2" title="Toggle sidebar" aria-expanded="true">
      <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="3" width="18" height="18" rx="2"/><line x1="9" y1="3" x2="9" y2="21"/></svg>
      <span class="toggle-text">Hide sidebar</span>
    </button>
  </div>

  <div class="mb-10 text-justify">
    I throw on the wheel and experiment with making my own glazes in my home studio. My favorite things to make are big vases and mugs. I'm drawn to nature-inspired textures - mosses, mushrooms, organic geometry - and I like building up lots of layers in both glaze and surface design. Lately I've been exploring ways to incorporate electronics into ceramic forms.
  </div>

  <div class="mb-6 flex gap-3">
    <a class="btn btn-outline border-primary text-primary hover:bg-primary hover:text-primary-content hover:border-primary transition-colors gap-2" href="https://www.instagram.com/dorion_ceramics" target="_blank" rel="noopener noreferrer">
      <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/></svg>
      Instagram
    </a>
    <a class="btn btn-outline border-accent text-accent hover:bg-accent hover:text-accent-content hover:border-accent transition-colors gap-2" href="https://etsy.com/shop/DorionCeramic" target="_blank" rel="noopener noreferrer">
      <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="9" cy="21" r="1"/><circle cx="20" cy="21" r="1"/><path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"/></svg>
      Shop
    </a>
  </div>

  <div class="gallery mb-10" id="gallery">
    {images.map((image, index) => (
      <article class="gallery-item">
        <button
          type="button"
          data-src={image.rawSrc}
          data-fancybox="gallery"
          data-caption={image.label}
          class="gallery-link-btn"
        >
          <Image
            src={image.src}
            alt={image.description}
            width={500}
            height={500}
            format="webp"
            quality={65}
            loading={index < 6 ? "eager" : "lazy"}
            densities={[1, 2]}
          />
        </button>
        <span class="label">{image.label}</span>
      </article>
    ))}
  </div>

  <script>
    // Development-only console warning (client-side version)
    function debugWarn(message: string, ...args: unknown[]): void {
      if (import.meta.env.DEV) {
        console.warn(message, ...args);
      }
    }

    // Fancybox loaded lazily on first gallery click
    // Use Promise pattern to handle race conditions on rapid clicks
    let fancyboxLoadPromise: Promise<void> | null = null;
    let FancyboxModule: typeof import("@fancyapps/ui").Fancybox | null = null;

    // AbortController for cleaning up event listeners on navigation
    let galleryAbortController: AbortController | null = null;
    let sidebarAbortController: AbortController | null = null;

    // Shared sidebar state management
    function getSidebarCollapsed(): boolean {
      try {
        return sessionStorage.getItem('pottery-sidebar-collapsed') === 'true';
      } catch {
        return false;
      }
    }

    function setSidebarCollapsed(collapsed: boolean): void {
      try {
        sessionStorage.setItem('pottery-sidebar-collapsed', collapsed ? 'true' : 'false');
      } catch {
        // Ignore storage errors (quota exceeded, etc.)
      }
    }

    function applySidebarState(collapsed: boolean): void {
      const drawer = document.querySelector('.drawer');
      // Single guard: check if drawer exists and is in DOM
      if (!drawer || !document.contains(drawer)) {
        debugWarn('applySidebarState: Drawer not available');
        return;
      }

      const toggleBtn = document.getElementById('sidebar-toggle');
      const toggleText = toggleBtn?.querySelector('.toggle-text');

      if (collapsed) {
        drawer.classList.add('pottery-sidebar-collapsed');
        if (toggleText) toggleText.textContent = 'Show sidebar';
        toggleBtn?.setAttribute('aria-expanded', 'false');
      } else {
        drawer.classList.remove('pottery-sidebar-collapsed');
        if (toggleText) toggleText.textContent = 'Hide sidebar';
        toggleBtn?.setAttribute('aria-expanded', 'true');
      }
    }

    // Client-side shuffle for static site deployment
    function shuffleGallery(): void {
      const gallery = document.getElementById('gallery');
      if (!gallery) return;

      const items = Array.from(gallery.querySelectorAll('.gallery-item'));
      if (items.length === 0) return;

      // Fisher-Yates shuffle
      for (let i = items.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        [items[i], items[j]] = [items[j], items[i]];
      }

      // Reorder DOM elements
      gallery.replaceChildren(...items);
    }

    async function loadFancybox(): Promise<void> {
      // Use Promise pattern to handle concurrent calls (rapid clicks)
      if (fancyboxLoadPromise) return fancyboxLoadPromise;

      fancyboxLoadPromise = (async () => {
        // Dynamically load CSS from CDN
        const link = document.createElement('link');
        link.rel = 'stylesheet';
        link.href = 'https://cdn.jsdelivr.net/npm/@fancyapps/ui@5.0/dist/fancybox/fancybox.css';
        document.head.appendChild(link);

        // Wait for CSS to load before initializing
        await new Promise<void>((resolve) => {
          link.onload = () => resolve();
          link.onerror = () => {
            debugWarn('Fancybox CSS failed to load');

            // Show user-facing warning
            const gallery = document.getElementById('gallery');
            if (gallery) {
              const warning = document.createElement('div');
              warning.className = 'alert alert-warning mb-4';
              warning.innerHTML = `
                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 shrink-0 stroke-current" fill="none" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
                </svg>
                <span>Gallery lightbox unavailable. Click images to view full-size in new tab.</span>
              `;
              gallery.insertAdjacentElement('beforebegin', warning);
            }

            resolve(); // Continue even if CSS fails - Fancybox still works
          };
        });

        // Dynamically import JS with error handling
        try {
          const { Fancybox } = await import("@fancyapps/ui");
          FancyboxModule = Fancybox;

          // Bind to gallery elements with proper configuration
          Fancybox.bind('[data-fancybox="gallery"]', {
            Thumbs: false,
            Toolbar: {
              display: {
                left: [],
                middle: [],
                right: ["close"],
              },
            },
            Images: { zoom: true },
            Carousel: { transition: "slide" },
            // Custom function to get image source from data-src attribute
            on: {
              beforeLoad: (fancybox: any, slide: any) => {
                const trigger = slide.triggerEl;
                if (trigger) {
                  const src = trigger.getAttribute('data-src');
                  if (src) {
                    slide.src = src;
                  }
                }
              }
            }
          });
        } catch (err: unknown) {
          const errorMsg = err instanceof Error ? err.message : String(err);
          debugWarn('Failed to load Fancybox JavaScript module:', errorMsg);
          fancyboxLoadPromise = null; // Reset so user can retry
        }
      })();

      return fancyboxLoadPromise;
    }

    function initGalleryClickHandler(): void {
      const gallery = document.getElementById('gallery');
      if (!gallery) return;

      // Abort any existing controller before creating new one
      galleryAbortController?.abort();
      galleryAbortController = new AbortController();

      gallery.addEventListener('click', async (e) => {
        const target = e.target as HTMLElement;
        const link = target.closest('[data-fancybox="gallery"]');
        if (!link) return;

        // If Fancybox not loaded yet, prevent default, load it, then trigger manually
        if (!fancyboxLoadPromise) {
          e.preventDefault();
          await loadFancybox();
          // After loading, trigger Fancybox manually
          if (FancyboxModule && link instanceof HTMLElement && document.contains(link)) {
            link.click();
          }
        }
        // If Fancybox is already loaded and bound, let it handle the click naturally
        // Fancybox will prevent the "#" navigation and open the lightbox
      }, { signal: galleryAbortController.signal });
    }

    function cleanupFancybox(): void {
      // Abort event listeners
      galleryAbortController?.abort();
      galleryAbortController = null;
      sidebarAbortController?.abort();
      sidebarAbortController = null;

      // Cleanup Fancybox
      if (FancyboxModule) {
        FancyboxModule.destroy();
        FancyboxModule = null;
      }
      fancyboxLoadPromise = null;
    }

    function initSidebarToggle(): void {
      const toggleBtn = document.getElementById('sidebar-toggle');
      const drawer = document.querySelector('.drawer');

      // Early returns before any operations
      if (!drawer || !toggleBtn) {
        if (!drawer) debugWarn('initSidebarToggle: Drawer element not found');
        if (!toggleBtn) debugWarn('initSidebarToggle: Toggle button not found');
        // Still apply saved state if drawer exists (even without toggle button)
        if (drawer) applySidebarState(getSidebarCollapsed());
        return;
      }

      // Restore saved state on init
      applySidebarState(getSidebarCollapsed());

      // Abort any existing controller before creating new one
      sidebarAbortController?.abort();
      sidebarAbortController = new AbortController();

      // Single click: toggle
      toggleBtn.addEventListener('click', () => {
        if (!drawer || !document.contains(drawer)) {
          debugWarn('Sidebar toggle click: Drawer not available');
          return;
        }
        const isCollapsed = drawer.classList.toggle('pottery-sidebar-collapsed');
        setSidebarCollapsed(isCollapsed);
        toggleBtn.setAttribute('aria-expanded', isCollapsed ? 'false' : 'true');
        const toggleText = toggleBtn.querySelector('.toggle-text');
        if (toggleText) {
          toggleText.textContent = isCollapsed ? 'Show sidebar' : 'Hide sidebar';
        }
      }, { signal: sidebarAbortController.signal });

      // Double click: reset to default (expanded)
      toggleBtn.addEventListener('dblclick', () => {
        sessionStorage.removeItem('pottery-sidebar-collapsed');
        applySidebarState(false); // Default state
        debugWarn('Pottery sidebar state reset to default');
      }, { signal: sidebarAbortController.signal });
    }

    // Shared initialization function (works with or without ViewTransitions)
    function initializePotteryPage(): void {
      if (!window.location.pathname.includes('/pottery')) return;

      shuffleGallery();
      initSidebarToggle();
      initGalleryClickHandler();
    }

    // For ViewTransitions: use astro events (if re-enabled in future)
    document.addEventListener('astro:after-swap', () => {
      const isPotteryPage = window.location.pathname.includes('/pottery');

      if (isPotteryPage) {
        initSidebarToggle();
      } else {
        // Leaving pottery page - restore normal sidebar state
        const drawer = document.querySelector('.drawer');
        if (drawer) {
          drawer.classList.remove('pottery-sidebar-collapsed');
        }
      }
    });

    document.addEventListener('astro:page-load', initializePotteryPage);

    // Fallback for when ViewTransitions is disabled (current state)
    document.addEventListener('DOMContentLoaded', initializePotteryPage);

    // If document is already loaded, initialize immediately
    if (document.readyState !== 'loading') {
      initializePotteryPage();
    }

    // Cleanup when navigating away (works with or without ViewTransitions)
    document.addEventListener('astro:before-swap', () => {
      cleanupFancybox();
    });

    // Also cleanup on page unload (fallback)
    window.addEventListener('beforeunload', () => {
      cleanupFancybox();
    });
  </script>
</BaseLayout>

<style>
  /* Sidebar toggle */
  :global(.drawer.pottery-sidebar-collapsed) {
    grid-template-columns: 0 1fr;
  }

  :global(.pottery-sidebar-collapsed .drawer-side) {
    transform: translateX(-100%);
    width: 0;
    opacity: 0;
    overflow: hidden;
  }

  :global(.pottery-sidebar-collapsed .drawer-content) {
    width: 100vw;
    max-width: 100vw;
  }

  :global(.pottery-sidebar-collapsed main) {
    max-width: 100%;
    width: 100%;
  }

  /* Photo Grid */
  .gallery {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: clamp(8px, 1vw, 16px);
  }

  .gallery-item {
    position: relative;
    aspect-ratio: 1;
    overflow: hidden;
    border-radius: 0.5rem;
  }

  .gallery-link-btn {
    /* Remove default button styling */
    background: none;
    border: none;
    padding: 0;
    margin: 0;
    font: inherit;

    /* Gallery item display */
    display: block;
    width: 100%;
    height: 100%;
    cursor: pointer;

    /* Match existing gallery styles */
    position: relative;
    overflow: hidden;
    border-radius: 0.5rem;
    transition: transform 0.2s ease;
  }

  .gallery-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.3s ease;
  }

  .gallery-item:hover img {
    transform: scale(1.02);
  }

  .gallery-item:hover {
    box-shadow: 0 0 0 3px theme('colors.accent');
  }

  .gallery-item .label {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 2rem 1rem 0.75rem;
    background: linear-gradient(to top, rgb(31 31 40 / 0.9) 0%, rgb(31 31 40 / 0) 100%);
    font-size: clamp(0.75rem, 1.5vw, 0.875rem);
    font-weight: 500;
    opacity: 0;
    transform: translateY(10px);
    transition: opacity 0.3s ease, transform 0.3s ease;
    pointer-events: none;
  }

  .gallery-item:hover .label,
  .gallery-item:focus-within .label {
    opacity: 1;
    transform: translateY(0);
  }

  @media (hover: none) {
    .gallery-item .label {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .gallery-link-btn:focus {
    outline: none;
  }

  .gallery-link-btn:focus-visible {
    outline: 2px solid #7E9CD8;
    outline-offset: 2px;
    border-radius: 0.5rem;
  }

  /* Fancybox customization */
  :global(.fancybox__container) {
    --fancybox-bg: rgb(31 31 40 / 0.98);
    --fancybox-accent-color: #C0A36E;
    --fancybox-color: #DCD7BA;
  }

  :global(.fancybox__backdrop) {
    background-color: rgb(31 31 40 / 0.98);
  }

  :global(.fancybox__toolbar) {
    --fancybox-color: #DCD7BA;
  }

  :global(.fancybox__nav button) {
    background: rgba(42, 42, 55, 0.7);
    color: #DCD7BA;
  }

  :global(.fancybox__nav button:hover) {
    background: rgba(42, 42, 55, 0.9);
    color: #C0A36E;
  }

  :global(.fancybox__caption) {
    font-size: 1rem;
    padding: 1rem;
    text-align: center;
    color: #DCD7BA;
    background: rgba(31, 31, 40, 0.9);
  }

  :global(.fancybox__slide) {
    padding: 44px 0;
  }

  :global(.fancybox__content) {
    background: transparent;
  }

  @media (prefers-reduced-motion: reduce) {
    .gallery-item img {
      transition: none;
    }

    .gallery-item .label {
      transition: none;
    }

    :global(.pottery-sidebar-collapsed .drawer-side) {
      transition: none;
    }
  }
</style>
