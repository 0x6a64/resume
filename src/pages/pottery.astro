---
import BaseLayout from "../layouts/BaseLayout.astro";
import { Image } from "astro:assets";
import "@fancyapps/ui/dist/fancybox/fancybox.css";

// Import all images from the pottery folder
const imageModules = import.meta.glob<{ default: ImageMetadata }>(
  "../images/pottery/*.{jpeg,jpg,png,gif,webp}",
  { eager: true }
);

// Convert to array and extract label from filename
const images = Object.entries(imageModules).map(([path, module]) => {
  const filename = path.split("/").pop() || "";
  const label = filename.replace(/\.(jpeg|jpg|png|gif|webp)$/i, "");

  return {
    src: module.default,
    label,
    filename,
  };
});

// Sort images alphabetically by label
images.sort((a, b) => a.label.localeCompare(b.label));
---

<BaseLayout title="Ceramics" sideBarActiveItemID="pottery">
  <div class="mb-5 flex justify-between items-start">
    <div>
      <div class="text-xl py-1">Dorion Ceramics</div>
      <div class="text-3xl w-full font-bold">Ceramics</div>
    </div>
    <button id="sidebar-toggle" class="btn btn-sm btn-ghost hidden lg:flex gap-2" title="Toggle sidebar">
      <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2"/><line x1="9" y1="3" x2="9" y2="21"/></svg>
      <span class="toggle-text">Hide sidebar</span>
    </button>
  </div>

  <div class="mb-10 text-justify">
    Belgian American potter inspired by nature, travel, and clean geometry. Currently exploring the intersection of electronics and ceramic forms.
  </div>

  <div class="mb-6 flex gap-3">
    <a class="btn btn-primary gap-2" href="https://instagram.com/dorion_ceramics" target="_blank">
      <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/></svg>
      Instagram
    </a>
    <a class="btn btn-outline border-accent text-accent hover:bg-[#C0A36E] hover:border-[#C0A36E] hover:text-base-100 gap-2" href="https://etsy.com/shop/DorionCeramic" target="_blank">
      <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M8.559 3.89c0-.31.32-.521.659-.521 1.867 0 4.877.183 6.549.652.854.242 1.267.643 1.267 1.137v2.32c0 .622-.482 1.097-1.096 1.097-.616 0-1.092-.475-1.092-1.097v-.797c0-.313-.208-.517-.513-.517h-3.461c-.312 0-.544.158-.544.478v5.116h2.459c.62 0 1.092.475 1.092 1.097s-.473 1.096-1.092 1.096h-2.459v5.117c0 .32.232.479.544.479h3.461c.305 0 .513-.205.513-.518v-.797c0-.622.476-1.096 1.092-1.096.614 0 1.096.474 1.096 1.096v2.32c0 .494-.413.896-1.267 1.138-1.672.469-4.682.652-6.549.652-.339 0-.659-.211-.659-.521v-17.93z"/></svg>
      Shop on Etsy
    </a>
  </div>

  <div class="gallery mb-10" id="gallery">
    {images.map((image, index) => (
      <article class="gallery-item">
        <a
          href={image.src.src}
          data-fancybox="gallery"
          data-caption={image.label}
        >
          <Image
            src={image.src}
            alt={image.label}
            width={image.src.width}
            height={image.src.height}
            format="webp"
            quality={80}
            loading={index < 12 ? "eager" : "lazy"}
          />
          <span class="label">{image.label}</span>
        </a>
      </article>
    ))}
  </div>

  <script>
    import { Fancybox } from "@fancyapps/ui";
    Fancybox.bind('[data-fancybox="gallery"]', {
      Thumbs: false,
      Toolbar: {
        display: {
          left: [],
          middle: [],
          right: ["close"],
        },
      },
      Images: { zoom: true },
      Carousel: { transition: "slide" },
    });

    // Sidebar toggle functionality
    const toggleBtn = document.getElementById('sidebar-toggle');
    const drawer = document.querySelector('.drawer');
    const toggleText = toggleBtn?.querySelector('.toggle-text');

    toggleBtn?.addEventListener('click', () => {
      drawer?.classList.toggle('lg:drawer-open');
      if (drawer?.classList.contains('lg:drawer-open')) {
        if (toggleText) toggleText.textContent = 'Hide sidebar';
      } else {
        if (toggleText) toggleText.textContent = 'Show sidebar';
      }
    });
  </script>
</BaseLayout>

<style>
  /* Photo Grid */
  .gallery {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: clamp(8px, 1vw, 16px);
  }

  .gallery-item {
    position: relative;
    aspect-ratio: 1;
    overflow: hidden;
    border-radius: 0.5rem;
  }

  .gallery-item a {
    display: block;
    width: 100%;
    height: 100%;
    text-decoration: none;
    color: inherit;
  }

  .gallery-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.3s ease;
  }

  .gallery-item:hover img {
    transform: scale(1.02);
  }

  .gallery-item .label {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 2rem 1rem 0.75rem;
    background: linear-gradient(to top, rgba(31, 31, 40, 0.9) 0%, rgba(31, 31, 40, 0) 100%);
    font-size: clamp(0.75rem, 1.5vw, 0.875rem);
    font-weight: 500;
    opacity: 0;
    transform: translateY(10px);
    transition: opacity 0.3s ease, transform 0.3s ease;
    pointer-events: none;
  }

  .gallery-item:hover .label,
  .gallery-item:focus-within .label {
    opacity: 1;
    transform: translateY(0);
  }

  @media (hover: none) {
    .gallery-item .label {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .gallery-item a:focus { outline: none; }
  .gallery-item a:focus-visible {
    outline: 2px solid theme('colors.primary');
    outline-offset: 2px;
  }

  /* Fancybox customization */
  :global(.fancybox__container) {
    --fancybox-bg: rgba(31, 31, 40, 0.98);
  }

  :global(.fancybox__toolbar) {
    --fancybox-color: theme('colors.base-content');
  }

  :global(.fancybox__caption) {
    font-size: 1rem;
    padding: 1rem;
    text-align: center;
  }
</style>
